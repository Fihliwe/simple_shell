#include <shell.h>

/*
 * The function contains parameters.
 * constant character array,
 * which gets passed the text that will be used to prompt the user.
 */

void main(void)
{
        char command[64]

        input_status
        get_user_input(const char *prompt, char *user_input, size_t size)
        while (1)
                {
                        show_prompt();
                        read_command, sizeof(command);
                        execute_command(command);
                }
        return 0
}
